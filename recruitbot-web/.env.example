# RecruitBot frontend environment (example — no secrets)
# Copy to .env.development / .env.production and adjust as needed.
VITE_API_BASE_URL=http://localhost:3000
VITE_APP_NAME=RecruitBot
VITE_ENABLE_MOCK=false
