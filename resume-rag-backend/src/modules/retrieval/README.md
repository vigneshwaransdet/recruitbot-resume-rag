# Retrieval Module

Keep this module empty until ingestion is complete and verified.

Retrieval will be implemented only after all ingestion phases pass, and
it must reuse the same backend server, environment, MongoDB connection,
and `resume_rag` collection created by the ingestion module.
